on:
  push:
    branches:
      - main
      - dev
  pull_request:
    branches:
      - main
      - dev
  workflow_dispatch:

jobs:
  build:
    uses: zmkfirmware/zmk/.github/workflows/user-build-matrices.yml@main
    with:
      config-path: config
